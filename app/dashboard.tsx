import { BaseView } from "@/components/common";
import { Text } from "react-native-paper";

export default function Dashboard(){
    return(
        <BaseView name="dashboard">
            <Text>Dashboard</Text>
        </BaseView>
    )
}