import { BaseView } from "@/components/common";
import { Text } from "react-native-paper";
export default function Settings(){
    return(
        <BaseView name="Settings">
            <Text>Settings</Text>
        </BaseView>
    )
}